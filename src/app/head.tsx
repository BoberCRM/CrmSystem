// src/app/head.tsx

import { FC } from 'react';
// import type { Metadata } from "next";

// export const metadata: Metadata = {
//   title: "Bober CRM",
//   description: "CRM system",
// };

const Head: FC = () => (
  <>
    <title>Bober CRM</title>
    <meta name="description" content="CRM system" />
  </>
);

export default Head;
